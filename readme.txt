✅ เหตุผลที่ต้องดูในระดับ เซกเตอร์ ทั้งที่ไฟล์ถูกเก็บเป็น คลัสเตอร์ มีดังนี้
1. ข้อมูลบางอย่างไม่ได้อยู่ในคลัสเตอร์ แต่ถูกจัดเก็บเป็นเซกเตอร์เดียว เช่น BPB
BPB (BIOS Parameter Block) หรือโครงสร้างข้อมูลระบบไฟล์จะอยู่ใน เซกเตอร์แรกของ SD card (มักคือ sector 0)
ดังนั้นต้องอ่าน เฉพาะเซกเตอร์นั้น เพื่อดูโครงสร้างระบบไฟล์ก่อน เช่น ขนาดเซกเตอร์,จำนวน FAT ,จุดเริ่มต้นของไดเรกทอรีราก
📌 จึงต้องอ่านระดับเซกเตอร์ เพื่อรู้ว่า "คลัสเตอร์" เริ่มตรงไหนและมีขนาดเท่าไหร่

2. FAT Table เองก็ถูกเก็บแบบต่อเนื่องเป็นเซกเตอร์ ไม่ใช่คลัสเตอร์
FAT (File Allocation Table) เป็นตารางที่เก็บว่า คลัสเตอร์ถัดไปของไฟล์อยู่ที่ไหน,คลัสเตอร์สุดท้ายหรือว่างคืออันไหน
ตาราง FAT นี้ไม่ได้เก็บเป็นคลัสเตอร์ แต่เก็บเป็น เซกเตอร์ต่อเนื่อง
เวลาเราจะไปดูว่า "คลัสเตอร์ถัดไปของไฟล์นี้คืออะไร" → ต้องไปอ่าน FAT → ซึ่งต้องอ่าน "เซกเตอร์" ตำแหน่งนั้น

3. บางฟังก์ชัน (เช่น read_sector) มีไว้เพื่อการเข้าถึงเฉพาะจุด
บางทีเราต้องการแค่ อ่านเซกเตอร์เดียวเฉพาะจุด (เช่น ดูข้อมูล FAT หรือ BPB)
ถ้าใช้ read_cluster ไปเลย จะอ่านข้อมูลเกินความจำเป็น และอาจอ่านผิดจุดได้
ดังนั้นจึงแยกฟังก์ชันไว้สองแบบ คือ
read_sector() → ใช้เมื่อต้องการความแม่นยำระดับเซกเตอร์
read_cluster() → ใช้เมื่อต้องการอ่านข้อมูลไฟล์ที่เก็บแบบคลัสเตอร์

| ทำไมต้องอ่านระดับเซกเตอร์?                             | คำอธิบาย           |
| -------------------------------------------------- | ----------------- |
| เพื่ออ่าน BPB ที่อยู่ในเซกเตอร์แรก                         | ใช้ `read_sector`  |
| เพื่ออ่าน FAT ที่เก็บในเซกเตอร์ต่อเนื่อง                     | ใช้ `read_sector`  |
| เพื่อเข้าถึงจุดข้อมูลเล็ก ๆ ที่ไม่ได้อยู่เต็มคลัสเตอร์              | ใช้ `read_sector`  |
| ส่วนการอ่านเนื้อหาไฟล์ ใช้ระดับคลัสเตอร์                    | ใช้ `read_cluster` |


โดยปกติ sector แรก (sector 0) คือ Boot Sector ซึ่งในระบบไฟล์ FAT32 จะเรียกว่า BPB (BIOS Parameter Block) 
— เป็นข้อมูลสำคัญเกี่ยวกับโครงสร้างของไฟล์ระบบ เช่น 
*จำนวนไบต์ต่อเซกเตอร์
*จำนวนเซกเตอร์ต่อคลัสเตอร์
*ขนาดของ FAT
*คลัสเตอร์เริ่มต้นของ root directory และอื่น ๆ


🔹 FAT32 Boot Sector Layout (ไบต์ที่ 0 ของ sector แรก)
| Offset| ขนาด | ชื่อฟิลด์                                  | คำอธิบาย                      
| ------| ---- | --------------------------------------- | --------------------------------------|
| 0x00  | 3    | Jump instruction                        | คำสั่ง jump ไปยัง boot code              
| 0x03  | 8    | OEM Name                                | ชื่อระบบหรือผู้สร้าง (เช่น "MSDOS5.0")       
| 0x0B  | 2    | ##  Bytes Per Sector                    | ขนาด sector เช่น 512                  
| 0x0D  | 1    | ##  Sectors Per Cluster                 | จำนวน sectors ต่อ cluster             
| 0x0E  | 2    | ##  Reserved Sectors                    | จำนวน sector ที่จองไว้ก่อน FAT       
| 0x10  | 1    | Number of FATs                          | จำนวน FAT tables (ปกติ 2)           
| 0x11  | 2    | Max root dir entries (FAT12/16)         | (FAT32 ไม่ใช้ ใช้ค่า 0)             
| 0x13  | 2    | Total sectors (FAT12/16)                | (FAT32 ไม่ใช้ ใช้ค่า 0)            
| 0x15  | 1    | Media descriptor                        | รหัสสื่อ (เช่น 0xF8 = ฮาร์ดดิสก์)   
| 0x16  | 2    | Sectors per FAT (FAT12/16)              | (FAT32 ไม่ใช้ ใช้ค่า 0)             
| 0x18  | 2    | Sectors per track                       | สำหรับ BIOS                       
| 0x1A  | 2    | Number of heads                         | สำหรับ BIOS                         
| 0x1C  | 4    | Hidden sectors                          | จำนวน sector ก่อน partition นี้      
| 0x20  | 4    | Total sectors (FAT32)                   | จำนวน sector ทั้งหมดใน partition      
| 0x24  | 4    | ##  Sectors per FAT (FAT32)             | ขนาดของ FAT table                    
| 0x28  | 2    | Flags                                   | บิตแสดงว่า FAT ไหน active           
| 0x2A  | 2    | FAT version                             | เวอร์ชันของ FAT (ปกติ 0)             
| 0x2C  | 4    | ##  Root Cluster (เริ่มที่ cluster ไหน)     | ค่าเริ่มต้น root directory         
| 0x30  | 2    | FSInfo sector number                    | sector ที่เก็บ FSInfo                
| 0x32  | 2    | Backup boot sector number               | sector สำรอง boot                    
| 0x36  | 12   | Reserved                                | ใช้ภายใน                              
| 0x42  | 1    | Drive number                            | ใช้ใน BIOS                           
| 0x43  | 1    | Reserved                                | -                                     
| 0x44  | 1    | Boot signature (0x29)                   | แสดงว่ามี serial number               
| 0x45  | 4    | Volume ID (serial number)               | หมายเลขประจำ volume                 
| 0x49  | 11   | Volume Label                            | ชื่อ volume                         
| 0x54  | 8    | File system type ("FAT32   ")           | ชนิดไฟล์                             
| ...   | ...  | Boot code                               | โปรแกรม boot                         
| 0x1FE | 2    | Boot sector signature (0x55AA)          | ต้องมีเพื่อบูตได้                    

ของที่ใช้ในโค้ดคือ (ฟังชั่น read_bpb() )
| ตัวแปร                    | Offset ที่ใช้    | จำนวนไบต์ | หมายเหตุ    |
| ------------------------ | ------------- | ---------| ----------- |
| `sector_size`            | `buffer[11]`  | 2 ไบต์    | Offset 0x0B |
| `sectors_per_cluster`    | `buffer[13]`  | 1 ไบต์    | Offset 0x0D |
| `reserved_sectors`       | `buffer[14]`  | 2 ไบต์    | Offset 0x0E |
| `number_of_fats`         | `buffer[16]`  | 1 ไบต์    | Offset 0x10 |
| `total_fat_sectors`      | `buffer[36]`  | 4 ไบต์    | Offset 0x24 |
| `root_directory_cluster` | `buffer[44]`  | 4 ไบต์    | Offset 0x2C |


| ชนิดข้อมูลใน C     | ขนาด (โดยทั่วไปบนระบบ 32/64-bit)  | หมายถึงอ่านจาก buffer\[] ยังไง       
| ---------------- | ------------------------------- | ------------------------------------- |
| `unsigned char`  | 1 ไบต์                           | อ่านแค่ไบต์เดียว (`buffer[x]`)       
| `unsigned short` | 2 ไบต์                           | อ่านจาก `buffer[x]` ถึง `buffer[x+1]` 
| `unsigned int`   | 4 ไบต์                           | อ่านจาก `buffer[x]` ถึง `buffer[x+3]` 
| `unsigned long`  | 4 หรือ 8 ไบต์ (แล้วแต่ระบบ)         | อ่านหลายไบต์ต่อเนื่องตามขนาด          

